# Function Fusioner

🎉 **Function Fusioner** is an interactive console program that allows users to process lists of numbers dynamically. Users can choose an operation—like doubling or squaring—to apply to each element in the list and see the results instantly.

---

## Features

- Input a custom list of numbers.
- Apply operations to each element:
  - Double the number.
  - Square the number.
- Display the processed results.
- Interactive loop: process multiple lists until you choose to exit.
- Input validation for numeric values.

---

## Requirements

- [Dart SDK](https://dart.dev/get-dart) installed on your machine.

---

## How to Run

1. Clone or download this repository.
2. Save the code in a file named `function_fusioner.dart`.
3. Open your terminal/command prompt.
4. Navigate to the folder containing `function_fusioner.dart`.
5. Run the program using the following command:

```bash
dart run function_fusioner.dart
